apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: portfolio-virtualservice
  labels:
    app: portfolio
spec:
  hosts:
    - "fake.hugecat.net"
  gateways:
    - portfolio-gateway

  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: portfolio
            port:
              number: 80

